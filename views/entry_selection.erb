<% 
  @addable = admin?(@course.id) || @course.allow_student_feeds 
  @admin = admin?(@course.id)
  @secondary_feeds_shareable = @addable
  @primary_context = @course
  @secondary_context = @user
  @selection_mode = true
  @return_url = params['return_url']
%>
<%= erb :_context_feeds %>
